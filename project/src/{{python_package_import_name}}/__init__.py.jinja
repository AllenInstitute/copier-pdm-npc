"""
    {{ project_name }}

    {{ project_description }}
    :author: {{ author_fullname }} <{{ author_email }}>
    :license: {{ copyright_license }}
"""
import importlib.metadata

logger = logging.getLogger(__name__)

__version__ = importlib.metadata.version("{{ python_package_import_name }}")

is_dotenv_found = dotenv.load_dotenv(
    dotenv.find_dotenv(usecwd=True)
)  # take environment variables from .env
logger.debug(f"environment variables loaded from dotenv file: {is_dotenv_found}")